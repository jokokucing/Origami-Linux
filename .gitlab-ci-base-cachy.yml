include:
  - local: .gitlab-ci-common.yml
stages:
  - build
build-base-image-cachy:
  extends: .bluebuild-job-template
  stage: build
  script:
    - bluebuild build --verbose --build-chunked-oci --compression-format zstd --push ./recipes/recipe-base-cachy.yml
